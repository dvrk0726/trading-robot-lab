# Shared Schemas

Formal schema definitions for shared contracts.

## Purpose

Contains machine-readable schemas (JSON Schema, YAML, or similar) for all shared contracts. Used for validation in both Lab and Runtime.
