# PROJECT_STATE

Дата последнего обновления: 2026-07-07
Репозиторий: `dvrk0726/trading-robot-lab`
Статус: начальная фиксация проекта

## Назначение файла

Этот файл фиксирует текущее состояние проекта: что уже сделано, что известно, какие решения приняты и какой следующий шаг.

Любой ИИ-агент или разработчик должен читать этот файл после `AI_CONTEXT.md`.

## Текущая цель

Сформировать надежную базу знаний и архитектурный каркас для разработки нового торгового робота.

На текущем этапе задача не в написании live-робота и не в подключении к брокеру. Задача — аккуратно отделить:

- исследование стратегии;
- архитектуру системы;
- риск-менеджмент;
- бэктест;
- симуляцию;
- будущий execution layer.

## Что уже сделано

### 1. Создан закрытый GitHub-репозиторий

Создан приватный репозиторий:

```text
trading-robot-lab
```

Владелец:

```text
dvrk0726
```

Назначение репозитория: хранить код, документацию, архитектурные решения, исследовательские материалы и контекст для ИИ-агентов.

### 2. Подключен доступ ChatGPT/GitHub

Доступ к репозиторию проверен.

Текущие возможности:

- чтение файлов;
- создание файлов;
- обновление файлов;
- работа с репозиторием через GitHub connector.

### 3. Создан `README.md`

В `README.md` зафиксировано базовое назначение репозитория: закрытая разработка архитектуры торгового робота, анализ стратегий, тестирование и управление рисками.

### 4. Создан `AI_CONTEXT.md`

Создан главный контекстный файл для ИИ-агентов.

В нем зафиксировано:

- назначение проекта;
- запрет воспринимать старый HFT-архив как готового робота;
- порядок разработки;
- базовая идея стратегии из старого проекта;
- требования к новой архитектуре;
- обязательные режимы `backtest`, `paper`, `live`;
- базовые правила безопасности;
- минимальный risk management;
- инструкция для других ИИ-агентов.

## Что известно про старый HFT-архив

Старый архивный проект был предварительно разобран в диалоге.

Вывод: это не готовый инструмент для запуска, а исторический технический материал.

Предварительно обнаружены элементы:

- C# / WinForms проект `new_robot_uralpro`;
- работа с QUIK DDE;
- работа с TRANS2QUIK;
- работа с Plaza2/ClientGate;
- торговая логика по RI;
- расчет синтетического индекса;
- расчет спреда;
- лимитные заявки;
- перестановка заявок;
- учет позиции;
- закрытие позиции;
- CSV-логи и старые результаты.

## Предварительный вывод по старому роботу

Старый робот имеет ценность как:

- учебный пример;
- источник архитектурных идей;
- источник торговой гипотезы;
- материал для восстановления логики старой стратегии.

Старый робот не имеет статуса готового боевого решения.

Причины:

- устаревшая инфраструктура;
- изменившийся рынок;
- неизвестная актуальная прибыльность;
- отсутствие современного risk engine;
- отсутствие replay-бэктеста;
- риск случайной отправки реальных заявок;
- необходимость адаптации под современные данные и интерфейсы.

## Рабочая гипотеза стратегии

Основная гипотеза из старого робота:

```text
Фьючерс RI может временно отклоняться от синтетического индекса, рассчитанного по корзине акций и валютному фактору.
```

Смысл:

1. Считать синтетический индекс.
2. Считать спред между фьючерсом и синтетическим индексом.
3. Оценивать нормальное состояние спреда.
4. Искать аномальные отклонения.
5. Входить лимитной заявкой только если риск и условия рынка допустимы.
6. Выходить при нормализации спреда или по защитным условиям.

Эта гипотеза должна быть заново проверена на современных данных.

## Принятые принципы

### Принцип 1. Backtest first

До любого live-подключения должен быть создан бэктест.

### Принцип 2. Strategy is not execution

Стратегия не должна напрямую отправлять заявки.

Стратегия формирует сигнал или намерение. Заявка проходит через:

```text
strategy -> risk -> execution -> broker/exchange
```

### Принцип 3. Live выключен по умолчанию

Любая новая система должна запускаться в безопасном режиме по умолчанию:

```text
TRADING_MODE=backtest
```

Live-режим должен требовать явного ручного включения.

### Принцип 4. Секреты не хранятся в GitHub

В репозиторий нельзя добавлять реальные `.env`, токены, ключи, пароли и брокерские доступы.

### Принцип 5. Все важные решения фиксируются

Архитектурные решения должны фиксироваться в папке `decisions/` в формате ADR.

## Текущее состояние репозитория

На текущий момент создано:

```text
README.md
AI_CONTEXT.md
PROJECT_STATE.md
```

Планируемая структура еще не создана полностью.

## Ближайшие задачи

### Задача 1. Создать `SECURITY.md`

Зафиксировать правила безопасности:

- что запрещено коммитить;
- как работать с `.env`;
- как хранить токены;
- какие файлы запрещены;
- как проверять проект перед загрузкой в GitHub.

### Задача 2. Создать `ROADMAP.md`

Описать этапы разработки:

1. Документация и анализ.
2. Архитектура.
3. Бэктест.
4. Данные.
5. Risk engine.
6. Paper trading.
7. Live gate.

### Задача 3. Создать `.gitignore`

Минимально исключить:

```text
.env
.env.*
*.log
*.zip
*.exe
*.dll
__pycache__/
.venv/
data/raw/
data/private/
logs/
secrets/
```

### Задача 4. Создать `.env.example`

Добавить только примерные переменные без реальных секретов.

### Задача 5. Создать структуру папок

Планируемые папки:

```text
docs/
decisions/
prompts/
research/
src/
tests/
data/
logs/
```

Так как GitHub не хранит пустые папки, внутри каждой папки можно создать `README.md` или `.gitkeep`.

### Задача 6. Создать первичные ADR

Минимальные ADR:

```text
decisions/ADR-0001-backtest-first.md
decisions/ADR-0002-no-live-by-default.md
decisions/ADR-0003-strategy-risk-execution-separation.md
```

## Открытые вопросы

### 1. Стек разработки

Пока не принято окончательное решение по языку и стеку.

Варианты для обсуждения:

- Python для исследования, бэктеста и анализа данных;
- C++/Rust/C# для низколатентного execution layer;
- PostgreSQL для сделок, заявок, результатов;
- ClickHouse/Parquet для больших исторических рыночных данных.

### 2. Источник рыночных данных

Нужно определить:

- где брать исторические тиковые данные;
- нужен ли стакан;
- какие инструменты тестировать;
- какой период данных использовать;
- как учитывать комиссии и проскальзывание.

### 3. Современное подключение к рынку

Пока не принято решение, будет ли проект ориентирован на:

- QUIK API;
- брокерский API;
- MOEX FAST/TWIME;
- только исследовательский бэктест без live-режима.

На текущем этапе этот вопрос преждевременный.

### 4. Реалистичность HFT

Нужно отдельно оценить, есть ли смысл строить именно HFT, потому что настоящий HFT требует:

- очень быстрых данных;
- низкой задержки;
- colocated infrastructure;
- профессионального risk control;
- учета комиссий, очереди заявок и конкуренции.

На текущем этапе правильнее говорить не "готовый HFT", а "исследовательская платформа для торговых стратегий".

## Следующий рекомендуемый шаг

Создать `SECURITY.md`, затем `.gitignore`, затем `ROADMAP.md`.

После этого создать папки `docs/`, `decisions/`, `prompts/`, `research/` и начать переносить туда структурированную информацию.
